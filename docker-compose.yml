services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/barhopping.db
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
    restart: unless-stopped
